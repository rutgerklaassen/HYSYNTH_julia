def run() {
  pick_marker()
}
def run() {
  while(left_is_clear()) {
    turn_right()
  }
}
def run() {
  if(markers_present()) {
    repeat(14) {
      pick_marker()
      put_marker()
    }
  }
}
def run() {
  turn_right()
}
def run() {
  ifelse(front_is_clear()) {
    ifelse(markers_present()) {
      while(not left_is_clear()) {
        move()
      }
    }
    else {
      turn_right()
    }
  }
  else {
    put_marker()
  }
}
